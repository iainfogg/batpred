from apps.predbat.predbat import PredBat as pb
import pytest_mock

# def test_call_notify(mocker):
    # mocker.patch('pb.call_notify', return_value='yo')
    # obj = object()
    # x = pb(obj)
    # x.call_notify('hi')
    # apps.predbat.predbat.PredBat.call_service.assert_called_once_with('hi')

